# Environment Variables Example
# Copy this to .env.local and fill in your values

# App Configuration
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Storyblok Configuration
NEXT_PUBLIC_STORYBLOK_API_URL=https://api.storyblok.com/v2/cdn
NEXT_PUBLIC_STORYBLOK_TOKEN=your-public-access-token-here
NEXT_PUBLIC_STORYBLOK_VERSION=published
NEXT_PUBLIC_STORYBLOK_MAIN_FOLDER=portfolio

# Storyblok Preview (Server-only - for draft mode)
STORYBLOK_PREVIEW_TOKEN=your-preview-access-token-here

# Storyblok Secrets (Server-only - for API routes)
STORYBLOK_PREVIEW_SECRET=your-preview-secret-here
STORYBLOK_REVALIDATE_SECRET=your-revalidate-secret-here

# Development
# SKIP_ENV_VALIDATION=false

# How to get these values:
# 1. NEXT_PUBLIC_STORYBLOK_TOKEN: Space Settings > Access Tokens > Public Token
# 2. STORYBLOK_PREVIEW_TOKEN: Space Settings > Access Tokens > Preview Token
# 3. STORYBLOK_PREVIEW_SECRET: Generate random string (e.g., openssl rand -base64 32)
# 4. STORYBLOK_REVALIDATE_SECRET: Generate random string (e.g., openssl rand -base64 32)
