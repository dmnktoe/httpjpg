# Environment Variables für lokale Entwicklung
# Kopiere diese Datei zu .env.local und fülle deine Werte ein

# App URL (für lokale Entwicklung)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Storyblok Configuration
NEXT_PUBLIC_STORYBLOK_API_URL=https://api.storyblok.com/v2/cdn
NEXT_PUBLIC_STORYBLOK_TOKEN=dein-public-access-token-hier
NEXT_PUBLIC_STORYBLOK_VERSION=published
NEXT_PUBLIC_STORYBLOK_MAIN_FOLDER=portfolio

# Storyblok Preview (Server-only - für draft mode)
STORYBLOK_PREVIEW_TOKEN=dein-preview-access-token-hier

# Storyblok Secrets (Server-only - für API routes)
# Generiere mit: openssl rand -base64 32
STORYBLOK_PREVIEW_SECRET=dein-random-preview-secret-hier
STORYBLOK_REVALIDATE_SECRET=dein-random-revalidate-secret-hier

# Optional: Google Analytics
# NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# Development
# SKIP_ENV_VALIDATION=false
