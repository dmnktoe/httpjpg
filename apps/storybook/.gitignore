# Storybook build output
storybook-static/

# Build output
storybook-static/

# Dependencies
node_modules/

# Build info
*.tsbuildinfo

# Visual regression — baselines live in the Actions cache / artifacts, never in the repo
tests/visual/__screenshots__/
visual-diffs/
playwright-report/
test-results/
visual-results.json
